'use client' // indicates a clint component
import React from 'react'

function AddToCart() {
  return (
    <div>
       <button onClick={() => console.log('test')}>Add to Cart</button>
    </div>
  )
}

export default AddToCart
